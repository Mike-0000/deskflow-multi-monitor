/*
 * Deskflow advanced monitor layout example configuration.
 *
 * Place monitors in world coordinates (density-independent layout units).
 * Partial overlaps are supported: only overlapping edge segments transition.
 * When advancedLayout = true, section: links is ignored for routing.
 */

section: screens
	desktop:
	laptop:
end

section: links
	# Optional legacy fallback only used when advancedLayout = false.
	desktop:
		right = laptop
	laptop:
		left = desktop
end

section: display_layouts
	version = 2
	advancedLayout = true
	desktop:
		main4k:
			id = DISPLAY1
			name = Main 4K
			worldX = 0
			worldY = 0
			width = 3840
			height = 2160
			layoutWidth = 2560
			layoutHeight = 1440
			localX = 0
			localY = 0
			scale = 1.5
			dpi = 144
		secondary:
			id = DISPLAY2
			name = Side Monitor
			worldX = 2560
			worldY = 400
			width = 2560
			height = 1440
			layoutWidth = 2560
			layoutHeight = 1440
			localX = 3840
			localY = 400
	laptop:
		builtin:
			id = DISPLAY1
			name = Laptop Display
			worldX = 2560
			worldY = 600
			width = 1920
			height = 1200
			layoutWidth = 1920
			layoutHeight = 1200
			localX = 0
			localY = 0
end

section: options
	relativeMouseMoves = false
	win32KeepForeground = false
end
